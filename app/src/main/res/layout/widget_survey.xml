<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:id="@+id/content"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:divider="@drawable/divider_horizontal_transparent_small"
              android:orientation="vertical"
              android:showDividers="beginning|middle">

    <TextView
        android:id="@+id/survey_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="@string/dummy_long"
        android:textColor="?contextColorText"/>

    <include
        layout="@layout/widget_survey_button"
        android:id="@+id/survey_button1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <include
        layout="@layout/widget_survey_button"
        android:id="@+id/survey_button1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</LinearLayout>