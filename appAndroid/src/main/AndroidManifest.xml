<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <application
        android:name=".ZumpaReaderApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/ThemeBlack"
        tools:ignore="GoogleAppIndexingWarning,UnusedAttribute">
        <!-- adjustNothing, not adjustResize: the window is edge to edge - enableEdgeToEdge in
             MainActivity clears decorFitsSystemWindows - so the screens read the keyboard as a
             WindowInsets.ime inside safeDrawing and pad themselves for it. Letting the platform
             resize the window as well is a second, unanimated adjustment for the same keyboard.

             The activity window only. The post and offline dialogs are windows of their own with
             their own softInputMode, which compose sets for them.

             The cost is api 26-29, where androidx derives the ime inset from exactly the resize
             this turns off and so reports it as zero. MainActivity puts the resize back there. -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- exported=false is what the FCM docs specify: the message is dispatched inside the
             app by the library's own receiver, so nothing outside needs to reach this service.
             Exported, any app on the device could hand it a MESSAGING_EVENT and post a
             notification that looks like it came from the forum. -->
        <service
            android:name=".MyFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${authority}"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
